import React, { memo } from 'react';
import { Handle } from 'reactflow';

export default memo(({ }) => {
  return (
    <>
      <div className="border h-40 w-20 bg-white dark:bg-gray-800">
        Custom Color Picker Node this is a test
      </div>
    </>
  );
});
